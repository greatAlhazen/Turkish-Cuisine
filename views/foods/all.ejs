<% layout('layout/boilerplate')%>
<div id="map" style="width: 100%; height: 500px"></div>

<% for (let food of foods){%>
<div class="card mb-4">
  <div class="row">
    <div class="col-md-5">
      <img class="img-fluid" alt="" src="<%=food.images[0].url%>" />
    </div>
    <div class="col-md-7">
      <div class="card-body">
        <h5 class="card-title"><%= food.title %></h5>
        <h5 class="card-title"><%= food.title %></h5>
        <p class="card-text">
          <small class="text-muted"><%= food.location%></small>
        </p>
        <a class="btn btn-warning" href="/foods/<%=food._id%>"
          >Go <%=food.title%></a
        >
      </div>
    </div>
  </div>
</div>
<% }%>

<script>
  const mapApi = "<%-process.env.MAP_API%>";
    // // prettier-ignore
  //nolint:gosec
  const foods = {features: <%- JSON.stringify(foods) %>};
</script>

<script src="/js/handleClusterMap.js"></script>
